## v0.1

底部导航不要使用路由，这部分使用子视图。用户可能频繁在几个底部导航之间对比数据来回跳转，不需要“返回”动画，路由开销太大了。

但是路由功能保留，之后的编辑记录 / 详情页，手动补录之类功能可能需要进行路由去切换。

稍微调整一下架构，底部导航改为子视图，main_shell.dart就不需要了，但是要保留路由功能，为之后的扩展功能。

## v0.2

目前实现了底部导航栏，主题颜色切换，路由功能扩展。这些已经实现的功能，在此基础上根据开发文档的给出的架构设计，重构架构，调整为Feature-First + MVVM架构，轻量化开发删去多余的东西。
