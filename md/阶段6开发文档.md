1. Navigator的改成GoRouter的pop和push
2. 管理页面「数据编辑管理」「同步设置」的图标小一点，字体大小和是否加粗（包括ui主题选择的字体）改成和「分类管理」一致
3.1 分类编辑页面：顶部四等分，「活动名称」改成「名称」，从左到右依次是图标 名称 权重 启用分类选项
3.2 主题颜色要做窄屏幕适配，要在屏幕里面完全显示。
3.3 图标库 选项改成共行一行全部显示，「工作学习」改成「学习」「休闲娱乐」改成「娱乐」窄屏适配。下面的图标选项一行最小5个。
4. 修改 sync_view_model.dart 中的 _SyncLifecycleObserver 和 SyncViewModel，在 App 暂停时取消定时器，恢复时重启定时器。针对解锁瞬间网络可能未就绪的问题，可以在 _handleResume 中增加一个小延迟，等待 1 秒，给系统网络模块一点恢复时间，当然还要再次检查状态，因为这 1 秒内用户可能切走了。
5. 配置一下字体 fontFamily: 'Roboto', 
  
  fontFamilyFallback: 
    // Windows: 微软雅黑
    'Microsoft YaHei', 
    // Linux: 文泉驿 (大多数 Linux 发行版都有)
    'WenQuanYi Micro Hei', 
    'Droid Sans Fallback',
    'Noto Sans CJK SC', // 如果 Linux 用户正好装了这个
    'sans-serif',
6. 写一下readme，详细说一下这个项目是干什么用的，怎么使用。